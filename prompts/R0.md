# Prompt R0 — Freeze contracts (no application features)

You are in the RepoScope repository. Read `AGENTS.md`, `docs/r0-decision-log.md`, `docs/versions.md`, and `docs/confinement-contract.md`.

Do not implement a scanner, parser, HTTP server, or UI.

Allowed work: decision log, version pins, JSON Schema, fixtures, expected adjacency, Cursor rules, wave prompts, and the R0 stop-gate report.

If versions or schema already exist, verify them against the TypeScript 6 compiler API constraint (no TypeScript 7 parser) and Node 24 LTS. Record any correction as an ADR.

Stop when `reports/r0-stop-gate.md` lists exact versions and states that no application feature code was added.
