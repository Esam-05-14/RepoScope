# Prompt R1 — Workspace and authenticated loopback shell

Create the npm workspaces listed in the blueprint (`apps/cli`, `apps/server`, `apps/web`, `packages/contracts`, `packages/parser-ts`, `packages/engine`, `packages/graph`). Pin versions from `docs/versions.md`. Commit a lockfile only if the user asks to commit.

Run spikes S1 and S4 from `docs/r0-compatibility-spikes.md`. If S1 fails, write an ADR and use the Vite 7 fallback.

CLI chooses a root (or `--demo` for bundled fixtures) and starts Fastify on 127.0.0.1 with a per-run session token. Add `GET /api/health` that requires the authorization header. Add a negative test for a missing or wrong token and a wrong Origin/Host.

No repository scan yet. Progress types must distinguish `idle | scanning | canceled | failed | partial | completed`.

Stop when a fresh `npm ci && npm run build` works and the auth tests pass.
