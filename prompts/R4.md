# Prompt R4 — Explorer, evidence, coverage

Build `/`, `/explore`, `/coverage` with a three-pane desktop layout and the responsive collapse rules in `docs/blueprint/09-api-and-ux.md`.

Cap the first graph at 250 nodes and 500 relations. Provide an accessible relationship list. Arrow direction must match stored `A -> B` means A imports B.

Evidence is read-only, hash-checked, and shows current / stale / unavailable. Keyboard: search, Escape, focus restore.

Browser tests: scan fixture, select `src/lib/money.ts`, open evidence, show coverage. Manual breakpoint note in the stop-gate report.

Stop when those tests pass. No marketing pages.
