# Prompt R7 — Package, verify, document

Add `npm run verify` that runs typecheck, lint, unit/integration, and non-interactive browser tests in a documented order.

Benchmark the 1,000-file fixture if present; publish measured numbers even when targets are missed.

README: project in development until P0 acceptance cases pass. Then use the structure in `docs/blueprint/15-github-presentation.md`. Add a real maintainer contact to `SECURITY.md` only if one exists.

Demo uses real engine output on public fixtures. No private repository artifacts.

Stop when a clean clone reproduces `npm ci && npm run verify` and the release checklist is evidence-backed.
