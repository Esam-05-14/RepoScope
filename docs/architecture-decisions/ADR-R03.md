# ADR-R03: File-level graph before symbol-level graph

**Status:** Accepted (R0)

Public claims stay understandable and testable. Symbol binding, calls, runtime dispatch, and dead-code analysis are separate projects. Do not imply them in UI copy.
