# Prompt R5 — Source-free snapshots and compare

Export/import source-free snapshots. Default export omits source text and absolute paths. Comparing a snapshot to itself is an empty semantic diff.

Compare baseline vs revised: exactly one added value edge `src/lib/money.ts → src/main.ts`, content change on `money.ts`, cycle-group change as in `fixtures/expected`.

Incompatible parser/scope/edge-policy pairs are `blocked` or a prominent warning — never a silent architecture change.

Stop when identical compare is empty and the revised fixture difference is exact.
