# Prompt R2 — Inventory, parser, observations, coverage

Implement confined inventory and `packages/parser-ts` with `typescript@6.0.3`. Run spike S2 before claiming resolution works.

Extract only static ESM import and export-from. Classify everything else. Resolve through the compiler host. Do not install inspected packages. Do not read outside the root.

Produce a validated source-free analysis object for `fixtures/esm-baseline` that matches `fixtures/expected/esm-baseline.json` sets. Add a negative test that a path outside the root is not read.

Stop when fixture outputs match and the out-of-root test passes. No graph UI.
