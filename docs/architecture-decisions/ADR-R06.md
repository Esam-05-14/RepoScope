# ADR-R06: Coverage is a first-class result

**Status:** Accepted (R0)

Unsupported constructs, exclusions, parse failures, and limits travel with every analysis. A partial graph must not look indistinguishable from a fully processed in-scope graph. Coverage stays one click from every impact view and is included in exports.
